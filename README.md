# laptrinhweb

Dự án demo web tĩnh **LJRUI Food** — ASP.NET Core MVC (`LjruiFoodWeb`).

## Chạy thử

```bash
cd LjruiFoodWeb
dotnet run
```

Mở trình duyệt tại địa chỉ `http://localhost:...` hiển thị trong terminal.
